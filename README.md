# 词径记 Web 版（PWA）

按需定制的背单词工具：只给你这一单元的词，用适合你的方法，按科学间隔安排复习。

> **当前进度：阶段 0 · B2（引导三页）已交付，并完成首页排版重做、图标改造（银质立体）、
> 悬浮球「小词」第一梯队（2026-09-19）**
> 方案与验收标准见工作区根目录的 `词径记-阶段0-实施方案-v1.md`。

## 这一批（B2）交付了什么

冷启动引导三步：**问答 → 20 词自测 → 结果页**。核心是让用户答完问题后，
看到一份"像给我排的"计划 —— 而且**每一条数字都能追到出处**。

| 路由 | 做什么 |
|---|---|
| `/onboarding` | 三题问答：目标 + 期限 / 每天时长 / 兴趣（多选 ≤3）。单选点击即进，无"下一步"按钮 |
| `/onboarding/test` | 20 词四选一，跨单元轮转抽题，**全程不显示对错** |
| `/onboarding/result` | 计划摘要 + 自测档位 + 方法摘要 + 4 条"为你改了什么" + 诚实标注 + 反馈入口 |
| `/` | 未完成引导自动跳 `/onboarding`；已完成则显示今日任务单 |

B2 新增的 6 个纯函数（全部有单测）：

| 函数 | 作用 |
|---|---|
| `buildQuiz` | 抽题：跨单元轮转 + 固定种子 PRNG（**同种子必得同卷**） |
| `scoreQuiz` / `scoreAnswers` / `bandFor` | 判分与三档（入门 / 中等 / 熟练） |
| `deadlineOptions` / `daysUntil` / `describeRemaining` | 期限选项与"还剩多久"（**要说"大概"，不说精确天数**） |
| `daysToCover` | 词数摊到每天 → 预计几天过一遍 |
| `dailyCapFor` | **分钟 → 每日词数**。结果页与首页共用它，两处数字永远一致 |
| `findGoal` / `isGoalSupported` | **目标是否已收录**的唯一判定出口（首页 / 问答页 / 结果页共用） |

**这两条是实施/收尾时顺手补的，值得留档**：

> **`dailyCapFor`** —— 原先 `daily_minutes` 根本没参与到排计划里，每日词量走的是写死的
> `daily_cap`。结果页会说"每天 36 个词"、首页却说 20 个，两处打架。补上唯一来源后，
> 两个页面永远一致。
>
> **`findGoal` / `isGoalSupported`** —— 原先首页、问答页、结果页各自写了一遍
> `GOAL_OPTIONS.find(o => o.code === goal)?.supported`，**三处真值来源，改一处必漏两处**。
> 收敛到 `questions.ts` 一处，并配单测把「阶段 0 只收录中考」这条产品约定钉死：
> 谁改了 `supported`，测试立刻变红。

**质量闸门（2026-09-19 深夜复验，全部通过）**

| 检查 | 结果 |
|---|---|
| `npm run test` | ✅ **12 个测试文件 / 175 个用例全绿** |
| `npm run typecheck` | ✅ 零错误 |
| `npx eslint --max-warnings=0 .` | ✅ **零错误零警告** |
| 四个路由真实请求（dev） | ✅ 全部 HTTP 200 |
| 三个图标文件与 `<link rel="icon">` | ✅ 均 HTTP 200，指向实际生成的文件（`favicon.ico` 实测 `[32,48,64,128]` / 21678 bytes） |
| 产物 CSS 类名核对 | ✅ 新用到的类名逐个在编译产物里查过（见下方「几条不能破的规矩」第 9 条） |
| 真实浏览器实测（Chromium，390×844 手机视口） | ✅ 进度环填充与配色、点开、Esc 关、点外部关、长按进开发者模式、拖拽吸附与位置记忆、收起→点回、告警光环、**开发者信息不泄漏**；`errors` 为空 |
| **面板越界**（球拖到 6 个高度 + 3 个边界场景） | ✅ 越界像素**全部为 0**（详见下节） |

## 小词 —— 左下角常驻悬浮球（2026-09-19 新增，首版叫「控制台」）

全站常驻的一颗银质「词」标，参考 Next.js 开发者指示器的形态：**安静地待在角落，
点开是一块面板**。它解决的是"系统在悄悄干活、用户看不见"的问题。

**它必须分两层，这是硬约束：**

| 层 | 内容 | 谁能看见 |
|---|---|---|
| **用户层** | 今日进度环、一键继续、动态提示 | 所有人（默认） |
| **开发者层** | 本地库规模、数据置信度、在线与否、动效是否降级、清空 | **长按 0.6 秒**才出现 |

> 开发者信息**绝不能漏给普通用户**（"数据置信度 0.75"对用户毫无意义，只会显得不专业）。
> 面板回归用户视角，措辞一律用大白话。

| 位置 | 作用 |
|---|---|
| `components/console/ConsoleDock.tsx` | 悬浮球本体：进度环、开合、**手势判定（点击 / 拖拽 / 长按）**、外部点击/Esc 关闭、网络监听 |
| `components/console/ConsolePanel.tsx` | 面板：今日任务 / 继续学习 / 动态流 /（开发者模式）运行状态与工具 |
| `lib/console/notices.ts` | ★ **纯逻辑**：去重插入、上限截断、时间格式化、告警判定（有单测） |
| `lib/console/dock.ts` | ★ **纯逻辑**：球的位姿（`{side, ratio}`）夹取、吸附、比例换算（有单测） |
| `lib/console/prefs.ts` | 本机偏好读写。**用 `useSyncExternalStore`，不要用 `useState + useEffect` 同步** |
| `lib/console/store.ts` | zustand store（全项目第一次真正用上 zustand）+ `pushNotice()` 便捷入口 |
| `lib/console/probe.ts` | 读本机状态：本地库规模 / 在线 / 动效是否降级 |
| `lib/plan/todayProgress.ts` | ★ **纯逻辑**：今日完成量（只读 `review_logs`，按本地日界过滤，有单测） |

**任何地方都能推一条提示**，一行搞定，不需要 hook、不需要 Provider：

```ts
import { pushNotice } from "@/lib/console/store";

pushNotice({ key: "daily-plan", level: "info", title: "今日任务单已排好", detail: "12 个词 · 约 15 分钟" });
```

`key` 是去重键：**同 key 的新提示会顶掉旧的**，所以"每次进首页推一条"不会滚成一屏。
`level` 取 `info | success | warning | danger`，后两档会让悬浮球亮起一圈柔光并带个小点。

### 实现上的三个坑（都实测踩过）

1. **进度环只能画在圆盘内侧。** `app/icon.png` 是**满幅圆**（圆边到画布边、四角透明），
   环画到外圈会被裁掉。所以环走内沿——正好当成金属盘的一圈表圈。
2. **球的位置必须用 `bottom` 锚定，不能用 `top`。** 面板是 `flex-col` 排在球**上方**的，
   容器锚在 `top` 会让面板一展开就把球往下顶。锚 `bottom` 则天然向上生长，球纹丝不动。
   同理，容器的水平对齐要跟着 `spot.side` 切 `items-start / items-end` ——
   写死 `items-start` 的话，球拖到右边再展开面板会**瞬间"跳"到面板左端**（实测踩过）。
3. **收起态那颗小点必须用实色品牌色 + 44px 热区。** 用 `bg-strong`(#d6cfc5) 半透明压浅底，
   实测对比度只有 1.9:1，等于**消失**（用户收起来就找不回来了）；12px 的触摸目标手指也点不中。

### 面板会在球的上下两侧换边（2026-09-19 修，重要）

面板默认排在球的**上方**。但球是可以拖的 —— 一旦球被拖进屏幕上半部，
"从球往上长"就等于把面板顶出屏幕。实测把球放到最上（顶边 16px）再点开：

```
面板顶边 −429px   而它总共才 435px 高   →   只有 6px 在屏幕里
```

两个原因叠在一起：

1. 方向写死了"永远往上"；
2. `max-h-[70vh]` 是相对**视口**算的，不是相对"球上方还剩多少"算的 ——
   它管不住"往上能长到哪"。

**修法也是两条，缺一不可：**

1. **换边。** 球在中线以下 → 面板往上长（默认落点，行为与改造前一致）；
   球在中线以上 → 面板往下长。判据是 `panelSideFor()`（`lib/console/dock.ts`，有单测）。
2. **面板高度按"那一侧真实剩余空间"封顶**，全用 CSS 表达式算：

   | 面板朝哪长 | max-height |
   |---|---|
   | 往上 | `min(70vh, 球上沿 − 缝 − 留白)` |
   | 往下 | `min(70vh, 100vh − 球下沿 − 缝 − 留白)` |

   这样面板**在任何位置都不可能伸出屏幕**，内容多了在面板内部滚动。
   而且因为表达式里全是 `vh`，**顺带省掉了 resize 监听** —— 转屏、
   手机地址栏收放全都自动跟上。

> 容器的锚定方向要跟着面板走：面板朝上 → 锚 `bottom`；面板朝下 → 锚 `top`。
> 锚错方向的话，面板一展开就会把球顶走。
>
> 为什么"中线 0.5"够准：精确分界是 `(视口高/2 − 球/2 − TOP_LIMIT) / 行程`，
> 代入 300~1400px 的视口落在 **0.500~0.509**。单测把这条近似钉死了，
> 而且就算判偏也不会出 bug —— max-height 是按那一侧真实空间算的。

实测（球拖到 6 个高度 + 3 个边界场景，越界像素全部为 0）：

| 球的位置 | 球顶边 | 面板 | 越界 |
|---|---|---|---|
| ratio 0（最上） | 16px | 下方 70–505 | 0 |
| ratio 0.49 | 390px | 下方 444–832 | 0 |
| ratio 0.50（中线） | 398px | **换成上方** 12–388 | 0 |
| ratio 1.0（最下，默认） | 780px | 上方 335–770 | 0 |

另测：右上角 + 开发者模式（面板最长 591px）→ 70–661 可滚动；
矮视口 390×480 → 70–406；横屏 844×390 → 横向 489–809、纵向 70–343。均为 0 越界。

> 面板长在下方时，入场方向也镜像了一下（`data-rise="down"` 把 `--motion-shift`
> 取负），这样它读起来是"从球里落下来"，而不是穿过球钻出来。
> 系统开了「减少动态」时 `animation: none`，位移一律不出。

### 手势判定：看位移，不看时长

| 动作 | 判定 |
|---|---|
| 点击 | 按下到抬起，位移 < 阈值 |
| 拖拽 | 位移 ≥ 阈值，松手吸附到最近的边，位置记在本机 |
| 长按 0.6 秒 | 进入 / 退出开发者模式 |

> 拖拽后**第一次点击会被吞掉**（防止刚拖完手一抖就弹面板）。这是设计，不是 bug。

> 效果预览（真实截图，390×844 手机视口）见工作区根目录 `词径记-小词-效果预览-v1.png`。
>
> **一个开发时的正常现象**：`npm run dev` 下页面左上角还有一颗 Next.js 自己的「N」指示器
> （它才是这次设计的参考对象）。那是 dev 专属的，`npm run build` 之后不会出现，
> 与本产品这颗「小词」标没有关系。

## 界面约定（排版 / 配色 / 图标）

**排版三层**（2026-09-19 立，新页面一律沿用）：

1. **字号阶梯**：主数字 44px ＞ 正文 14px ＞ 辅助 12px ＞ 标签 11px（标签配 `tracking-[0.08em]`）
2. **颜色三层**：`primary`（正文）＞ `secondary`（辅助）＞ `tertiary`（最安静）
3. **留白节奏**：卡间 12px、卡内 20~24px；**视觉重心手法只有一种 —— 主卡用 `bg-feature` 暖砂底衬**

两处刻意不做：**卡片投影**、**装饰性线条**。柔感靠描边 + 留白。

> **底衬色必须是暖色（`bg-feature` #ece5da），不能用主色的浅版（`brand-50`）。**
> 整页是暖白调，冷蓝灰的 `brand-50` 放上去会发青、跟页面打架（Andy 2026-09-19 指出）。
> 改动前后对比见工作区根目录 `词径记-首页主卡配色-改动前后-v1.png`。
> `brand-50` 仍用于**选项卡选中态**，那是它的正当用途，别一起换掉。

**图标**（`app/icon.png` / `app/apple-icon.png` / `app/favicon.ico`）：

- 设计语言：**深墨圆 + 银字「词」**，参考 Next.js 默认图标那种"深底亮标"的做法，
  再往**金属浮雕**推了一步。三处细节共用**同一个光源（左上）**，所以能"立"起来：
  底盘（左上亮、右下暗 + 贴边一道细高光）／笔画（左上沿亮、右下沿暗的倒角）／
  字形（右下角渐隐化进底盘，取自 Next.js「N」的做法）。
- **字是"银"不是"白"**：竖直方向有明暗渐变（上亮下暗）。平涂的白在深底上像贴纸，
  有明暗层次才像一块金属。
- **图标是生成物，不要手改 PNG。** 改造型 / 配色改 `scripts/gen-app-icon.py` 后重跑：

```bash
"C:/Users/wangh/.workbuddy/binaries/python/envs/default/Scripts/python.exe" scripts/gen-app-icon.py
```

> 脚本头部记了 11 条实测结论（为什么用圆不用圆角方形、为什么字号要分两档、
> 为什么光向必须统一、为什么 `.ico` 得自己拼而不是用 Pillow 的 `sizes=`），改之前先读一遍。

**尺寸档位（2026-09-19 定，含一次实测推翻）**

| 尺寸 | 用在哪 | 效果 |
|---|---|---|
| 512 | PWA 安装图标 / 高清屏 | 清晰 |
| 180 | iOS「添加到主屏幕」 | 清晰 |
| 128 | 高 DPI 标签页 / 书签栏大图标 | 清晰 |
| 64 | 标签页 · 高分屏（DPR 2） | 清晰 |
| 48 | 标签页 · 普通屏 / Windows 小图标 | 清晰 |
| 32 | 标签页最常遇到的一档 | 可辨认（**已专门优化**） |
| ~~16~~ | —— | **已移除** |

> **16px 为什么彻底不做了**：7 画汉字在 16 个像素里必然粘连。并排实测过三种做法
> （专门档 / 48→16 LANCZOS / 48→16 BOX）——**三种都糊，而且"专门档"糊得最凶**：
> 它为了撑开结构把字号放大到 62%，笔画直接粘成一块白斑。反倒是**不提供 16 档、
> 让浏览器把 48 档缩下来**，笔画层次更分明。所以取舍是：与其给一个更糟的小图，不如不给。
>
> **32px 反过来不能去掉**：它比"64 档缩下来"清楚得多（「讠」和「司」分得开），
> 是唯一被特殊照顾的档位（字号 62%、不上细节）。
> 曾经误判它"糊"——那是预览图把它放大了 4.7 倍显示造成的错觉。
>
> 完整说明图（含 1:1 真实大小对照）见工作区根目录 `词径记-图标尺寸说明-v2.png`，
> 由 `scripts/make-icon-guide.py` 从**真实产物文件**生成，改图标后重跑一次即可同步。


## 环境要求

- **Node.js ≥ 20**（开发机实测 v22.22.2 可用）
- npm（随 Node 附带）

## 第一次跑起来

```bash
# 1. 进工程目录（注意路径里有中文，命令要加引号）
cd "E:\编程\词径记PWA网页应用\cijingji-web"

# 2. 装依赖
npm install

# 3. 跑单测（必须全绿，这是每次交付的闸门）
npm run test

# 4. 起开发服务器，浏览器打开 http://localhost:3000
npm run dev
```

其他常用命令：

```bash
npm run build      # 构建（同时做类型检查）
npm run typecheck  # 只做类型检查
npm run lint       # 代码检查
npm run seed       # 重新生成 mock 种子（改了源词表才需要跑）
```

## 依赖源说明

工程里的 `.npmrc` 把 npm 源指向了国内镜像 `registry.npmmirror.com`：
官方源在开发机上实测**安装会长时间卡住**（15 分钟无进展）。
想换回官方源：删掉 `.npmrc`，或临时加 `--registry=https://registry.npmjs.org`。

## 首次安装会发生什么（2026-09-18 实测数据）

| 项目 | 实测值 | 说明 |
|---|---|---|
| 直接依赖 | 8 个 | next / react / react-dom / dexie / dexie-react-hooks / zustand / zod / framer-motion |
| 实际安装包数 | **401 个** | 传递依赖展开后的结果，Next 生态的正常量级 |
| 磁盘占用 | **3.2 GB / 23074 个文件** | 其中 `node_modules/next` 一个包就占 **1.2 GB**（`next/dist` 编译产物） |
| 首次安装耗时 | **约 34 分钟** | 偏长但有解释，见下 |
| 安全漏洞 | **0 个**（含开发依赖） | 2026-09-18 实测 |

**耗时为什么这么长**：`node_modules` 有 2.3 万个小文件，**Windows Defender 的实时保护会逐个扫描**，
这是最主要的拖慢源（通常拖慢 3~5 倍）。一次性成本，装完不用再装。

**两条容易被误会的输出，都不是错误**：

- `154 packages are looking for funding` —— 只是告诉你有 154 个包的作者填了赞助方式，
  纯信息提示，**跟你的代码、功能、花钱都无关**，不用跑 `npm fund`。
- 安装末尾的黄色 `warn` —— 第三方库的过时提示。**只要没有 `npm ERR!` 开头的红色报错就算成功。**

**`npm audit` 在国内镜像下用不了**：镜像不实现审计接口（报 `NOT_IMPLEMENTED`）。
需要时临时切官方源：

```bash
npm audit --registry=https://registry.npmjs.org
```

## 目录结构（阶段 0 相关部分）

```
app/
├── (app)/page.tsx            首页 / 今日任务单（未引导则跳 /onboarding）
├── onboarding/
│   ├── layout.tsx            引导流程共享外壳
│   ├── page.tsx              第 1 页：三题问答
│   ├── test/page.tsx         第 2 页：20 词自测
│   └── result/page.tsx       第 3 页：结果（最关键的情绪时刻）
├── layout.tsx                根布局（含微信 UA 降级的首帧内联脚本 + 全站常驻「小词」）
├── globals.css               ★ design tokens 全在这里（Tailwind v4 的 @theme）
├── icon.png                  图标 512×512（脚本生成，勿手改）
├── apple-icon.png            图标 180×180（同上）
└── favicon.ico               图标 32/48/64/128 多档（同上，**故意不含 16**）
components/
├── onboarding/
│   ├── StepProgress.tsx      分段式进度条
│   └── OptionCard.tsx        可点卡片（选项 / 词卡共用）
└── console/
    ├── ConsoleDock.tsx       左下角「小词」悬浮球（进度环 / 手势判定 / 开合 / 网络监听）
    └── ConsolePanel.tsx      展开后的面板（用户层 + 长按才现的开发者层）
lib/
├── console/
│   ├── notices.ts            ★ 纯函数：提示去重插入 / 上限截断 / 时间格式化 / 告警判定
│   ├── dock.ts               ★ 纯函数：球的位姿夹取 / 吸附 / 比例换算
│   ├── prefs.ts              本机偏好（useSyncExternalStore，勿用 useEffect 同步）
│   ├── store.ts              zustand store + pushNotice() 一行式入口
│   └── probe.ts              读本机状态（库规模 / 在线 / 动效降级），不发网络请求
├── db/
│   ├── types.ts              12 张表的类型（字段名与未来 Supabase 逐字一致）
│   ├── local.ts              Dexie 本地库（IndexedDB）
│   ├── repo.ts               profiles 读写封装（阶段 1 换 Supabase 只动这里）
│   ├── seed.ts               幂等灌种子
│   └── seed-data.json        构建产物：由样张转换而来，不手改
├── onboarding/
│   ├── questions.ts          引导题目数据（**是数据不是代码**）
│   ├── quiz.ts               ★ 纯函数：抽题 / 判分 / 分档
│   ├── deadline.ts           ★ 纯函数：期限选项与剩余天数
│   └── attempt.ts            作答的跨页传递（sessionStorage）
├── scope/
│   ├── schema.ts             scope_json 的 zod 校验
│   └── resolveScope.ts       ★ 纯函数：范围 → 一批词
├── plan/
│   ├── buildDailyPlan.ts     ★ 纯函数：一批词 → 今日任务单
│   └── estimate.ts           用时估算参数 + dailyCapFor（要调只改这里）
├── profile/mergeProfile.ts   ★ 纯函数：三层优先级 overrides > phase > goal_profile
├── ua/wechat.ts              微信 UA 检测 + 首帧降级脚本
└── design/tokens.ts          动效与圆角（颜色只在 globals.css，避免两处真相）
scripts/build-seed.mjs        样张 JSON → seed-data.json
scripts/gen-app-icon.py       ★ 图标生成（单源）：改图标只改这里，别手改 PNG
scripts/make-icon-guide.py    从真实图标产物生成「尺寸说明书」配图（文档用）
```

每个 `lib/` 模块旁边都有同名的 `.test.ts`（跑在真实种子数据 + 构造数据上）。
另有 `lib/onboarding/flow.test.ts`：**把"引导答完 → 排出一份今日任务单"整条数据链路串起来测**，
用来守住"两页数字打架"这类跨模块问题。

## 几条不能破的规矩（改代码前先读）

1. **AI Key 只在服务端**。前端代码、客户端组件、`NEXT_PUBLIC_*` 里永不出现任何 Key。
   `.env.local` 已在 `.gitignore` 中，**不要提交它**。
2. **禁硬编码颜色**：颜色只有一份来源 —— `app/globals.css` 的 `@theme` 块，
   组件里只用 `bg-surface` / `text-secondary` 这类语义类名。
3. **禁 emoji、禁紫粉渐变、禁 bounce**；动效只用 `translate` / `scale` / `opacity`，
   **禁 3D transform、禁 `will-change`**（微信内核会白屏）。
4. **复习调度将来由 FSRS 独占**：`pace` 配置里不许出现任何复习间隔参数。
5. **`resolveScope` / `buildDailyPlan` / `mergeProfile` 必须是纯函数**：无 AI、无 IO、可单测。
6. 数据要诚实：`confidence < 0.8` 的词表，界面上必须标注"可能不准"并给反馈入口。
7. **同一个判断只允许有一处实现**。发现两个地方写了同样的 `find` / 同样的阈值 /
   同样的映射，就抽成 `lib/` 里的具名函数并加单测 —— 三处真值来源 = 改一处必漏两处。
8. **一次只改一个文件的一处**：批量编辑同一文件时，前一处会被后一处覆盖（实测踩过）。
9. **Tailwind 类名要在产物里核对**：类名写错**不会报错，只会静默失效**，三道闸门一个都抓不到。
   改完样式要从产物 CSS 里 grep 一次。注意 CSS 里类名带反斜杠转义
   （`.text-\[11px\]`），`grep -F "text-[11px]"` 匹配不到，要用宽松子串（如 `11px`）。

   ```bash
   # ① 从 HTML 里拿本次构建真正的 CSS 文件名（带哈希，别硬编码）
   curl -s http://localhost:3000/ > /tmp/h.html
   CSS=$(grep -o '/_next/static/chunks/[^"]*app_globals[^"]*\.css' /tmp/h.html | head -1)
   # ② 逐个 grep 新用到的类名，出现次数应为 ≥1
   curl -s "http://localhost:3000$CSS" | grep -c -F "shadow-float"
   ```

   > **实测实例（2026-09-19）**：新写的 `duration-base` / `duration-fast` 计数为 **0** ——
   > Tailwind v4 只认 `duration-<数字>`，`--duration-*` 不是它的命名空间。
   > 已改成 `duration-160` / `duration-200`，并在 `globals.css` 的变量旁写了警告。
   >
   > **另一个坑**：dev 模式下产物 CSS **不压缩、规则跨多行**，
   > 所以 `grep -o '\.cls[^}]*}'` 这类"一行内吃完整条规则"的写法匹配不到
   > （`}` 在下一行）。查**整条规则**要先 `grep -n` 定位再往下读几行；
   > 只查"类名在不在"用宽松子串就够了。
   > 同一天里，`[data-animated][data-rise="down"]` 这条规则就因此被误判成"没生成"过一次。
10. **资源和图标都是"生成物"**：`seed-data.json`、`icon.png`、`favicon.ico` 都由脚本产出。
    要改就改脚本重跑，直接编辑产物会在下次重跑时被覆盖。
11. **读浏览器侧的外部状态用 `useSyncExternalStore`，不要 `useState + useEffect`。**
    `localStorage` / `matchMedia` / `navigator.onLine` 这类"React 之外的状态"，
    用 effect 同步进来是 React 官方明确不建议的写法（会多一次渲染、还有撕裂风险）。
    `lib/console/prefs.ts` 就是为此重写过一版，eslint 抓过一次。
12. **浮层的位置锚 `bottom`、对齐跟着所在边切。**
    面板排在球上方 → 锚 `top` 会把球往下顶；容器写死 `items-start` →
    球拖到右边再展开会"跳"到左边。两处都实测踩过，改浮动组件前先读 `ConsoleDock.tsx` 顶部注释。
13. **浅底上的小标记要用实色品牌色，并给足 44px 热区。**
    `bg-strong`(#d6cfc5) 那种"低调的浅灰"压白底只有 1.9:1 对比度，
    实测等于**看不见**（收起后的球找不回来就是这个问题）。
    图形元素至少 3:1，触摸目标至少 44px。
14. **浮层（follow 元素走的弹窗）必须能换边，并按那一侧的剩余空间封顶。**
    写死"永远朝上"或只用 `max-h-[70vh]`，只要触发它的元素能被拖到屏幕上半部，
    弹窗就一定会有内容被顶出可视区 —— 实测教训见上文「面板会在球的上下两侧换边」。
    判据做成纯函数（`panelSideFor`）以便单测；封顶高度用 `vh` 表达式在 CSS 里算，
    这样转屏不用写 resize 监听。
